<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 부모 노릇을 할 인터페이스를 지정 -->
<!-- implements IAdminReportDAO -->
<mapper namespace="com.haroot.mybatis.IAdminReportDAO">
	<!-- select 쿼리 →<select id="메소드 이름"></select> -->
	<!-- 고유해야 하기 때문에(식별가능해야하기때문에 id 속성) -->
	<!-- resultType 은 각각 하나씩 바인딩 되는 곳 -->	
	
	
	
	<!-- 미처리 게시판 게시물 수 -->
	<select id="boardNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		  FROM REPORTVIEW
		 WHERE REP_TYPE_CODE = 1
		   AND REP_STATE_CODE = 1
	</select>
	
  	<!-- 미처리 게시판 댓글 수 -->
  	<select id="boardCommNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		  FROM REPORTVIEW
		 WHERE REP_TYPE_CODE = 2
		   AND REP_STATE_CODE = 1
	</select>
	
	<!-- 미처리 산책메이트 방 수 -->
	<select id="walkNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM REPORTVIEW
		WHERE REP_TYPE_CODE = 3
		  AND REP_STATE_CODE = 1
	</select>
	
	<!-- 미처리 산책메이트 댓글 수 -->
	<select id="walkCommNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM REPORTVIEW
		WHERE REP_TYPE_CODE = 5
		 AND REP_STATE_CODE = 1
	</select>
	
	<!-- 미처리 오프라인신고 의견 수 -->
	<select id="walkOffNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM REPORTVIEW
		WHERE REP_TYPE_CODE = 4
		 AND REP_STATE_CODE = 1
	</select>

	<!-- 미처리 고객문의 수 -->
	<select id="askNotHandled" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM ASKVIEW
		WHERE ASK_STATE_CODE = 1
	</select>
	
	<!-- 게시판 게시물 신고리스트 출력 -->
	<select id="adBoardReportList" resultType="com.haroot.mybatis.AdminReportDTO">
		
		SELECT REP_LOG_CODE 
		    , REP_TYPE_CODE
		    , REP_TYPE_CONTENT
		    , SID_CODE
		    , REP_LOG_DATE
		    , REP_STATE_CODE
		    , REP_STATE_CONTENT
		    , REP_LOG_READ
		    , BOARD_REP_CODE
		    , BOARD_CODE
		    , BOARD_WRITER
		    , BOARD_TITLE
		    , BOARD_CONTENT
		FROM REPORTVIEW
		WHERE REP_TYPE_CODE = 1
		ORDER BY REP_LOG_CODE 
		
	</select>
	
	<!-- 산책메이트 방 신고리스트 출력 -->
	<select id="adWalkroomReportList" resultType="com.haroot.mybatis.AdminReportDTO">
		SELECT REP_LOG_CODE 
			    , REP_TYPE_CODE
			    , REP_TYPE_CONTENT
			    , SID_CODE
			    , REP_LOG_DATE
			    , REP_STATE_CODE
			    , REP_STATE_CONTENT
			    , REP_LOG_READ
			    , WALKROOM_REP_CODE
			    , WALKROOM_LEADER
			    , WALKROOM_CODE
			    , WALKROOM_TITLE
			    , WALKROOM_WORDS
		FROM REPORTVIEW
		WHERE REP_TYPE_CODE = 3
		ORDER BY REP_LOG_CODE
	</select>
	
	<!-- 고객문의(고객지원) 리스트 출력 --> 
	<!-- 이건..ReportDAO에서 해야할 일이군.. -->
	
	<!-- 
	<select id="adAskList" resultType="com.haroot.mybatis.AdminReportDTO">
		SELECT ASK_CODE
		    , ASK_CATE_CODE 
		    ,ASK_CATE_CONTENT
		    , SID_CODE
		    , MEM_NICKNAME
		    , ASK_DATE
		    , ASK_TITLE
		    , ASK_CONTENT
		    , ASK_PHOTO
		    , ASK_STATE_CODE
		    , ASK_STATE_CONTENT
		    , ASK_READ
		FROM ASKVIEW
		ORDER BY ASK_CODE
	</select> 
	-->
	
	
	<!-- 
	<select id="" resultType="com.haroot.mybatis.AdminReportDTO">
		
	</select>
	 -->
	 
	 
	<!-- 
	<insert id="add"> memberDTO 클래스의 #{ 속성 } 으로 넘김
		INSERT INTO TBL_MEMBERLIST(MID, NAME, TELEPHONE)
		VALUES(MEMBERLISTSEQ.NEXTVAL, #{name}, #{telephone}) 
	</insert>
	
	<delete id="remove">
		DELETE 
		FROM TBL_MEMBERLIST
		WHERE MID = #{mid}
	</delete>
	
	<update id="modify">
		UPDATE TBL_MEMBERLIST
		SET NAME = #{name}, TELEPHONE = #{telephone }
		WHERE MID = #{mid}
	</update> 
	-->
</mapper>