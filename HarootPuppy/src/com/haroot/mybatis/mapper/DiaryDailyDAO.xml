<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.haroot.mybatis.IDiaryDailyDAO"> <!-- 부모 노릇을 할 인터페이스 -->
	
	<insert id="addDaily" >
		INSERT INTO TBL_DAILY (DAILY_CODE, RELATION_CODE)
		VALUES(
		(SELECT CONCAT('DAILY', LPAD(NVL(SUBSTR(MAX(DAILY_CODE),6),0)+1, 3,0))
		FROM TBL_DAILY), 
		(SELECT RELATION_CODE
		FROM TBL_RELATION
		WHERE PET_CODE = #{PET_CODE})
		)
	</insert>
	
	

	
	
</mapper>