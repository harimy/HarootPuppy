<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.haroot.mybatis.IBoardViewDAO">
	<select id="commView" parameterType="map" resultType="com.haroot.mybatis.BoardViewDTO">
		SELECT NICKNAME, BOARD_COMM_CONTENT, TO_CHAR(BOARD_COMM_DATE,'YYYY-MM-DD HH24:MI:SS') AS BOARD_COMM_DATE
		FROM BOARDVIEW
		WHERE NUM = #{num}
		AND NICKNAME = #{nickname}
	</select>
</mapper>